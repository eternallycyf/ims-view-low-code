import React, { FC, useEffect, useRef, useState } from 'react';

interface EditAreaProps {}
const EditArea: FC<EditAreaProps> = (props) => {
  const {} = props;
  return <div>EditArea</div>;
};
export default EditArea;
